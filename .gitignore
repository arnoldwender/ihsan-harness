node_modules/
.DS_Store
*.log

# Keep the repo's own assets/ tracked despite the case-insensitive global /Assets/ ignore (macOS)
!/assets/

# The test suite is Python; its caches are build output, not source.
__pycache__/
.pytest_cache/
